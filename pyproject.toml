[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "citizenship-search-app"
version = "0.2.0"
description = "Restarted discovery-first family search app for Polish citizenship research."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
citizenship-search = "citizenship_search.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
